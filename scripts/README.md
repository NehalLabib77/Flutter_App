# Scripts

Ad-hoc ops scripts that don't belong inside `frontend/`, `backend/`, or `ml/`.

Current contents: _empty._

Add new scripts here as standalone files with a clear docstring describing
inputs, outputs, and how to invoke them. Prefer Python 3.11+ so the same
venv as the backend can run them.