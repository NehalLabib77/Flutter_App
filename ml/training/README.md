# Training

Training scripts for the EduCompass recommender.

Current contents: _empty._

Expected layout once populated:

```
ml/training/
├── train_v2.py            builds ml/artifacts/models/v2/*.joblib
├── evaluate.py            writes to ml/evaluation/
└── README.md              (this file)
```

Use `ml/notebooks/` for exploration; promote stable code here once it
produces reproducible artefacts.